# Benchmark scope

No workload benchmark or latency result has been measured. Add representative
workloads only after their correctness contracts are approved. Record hardware,
compiler, dependencies, flags, data hashes, precision and durability assumptions,
raw samples and distribution summaries. Compilation time and a passing bootstrap
CLI test are not quant-platform performance evidence.
